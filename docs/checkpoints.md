# Development Workflow

Development proceeds checkpoint by checkpoint.

Each checkpoint must:

- compile
- preserve previous functionality
- be independently testable
- stop after completion

Every checkpoint requires:

1. Build verification.

2. Manual runtime verification.

3. Approval.

Only after approval may the next checkpoint begin.

Large multi-feature commits are prohibited.

Architecture changes require documentation updates.